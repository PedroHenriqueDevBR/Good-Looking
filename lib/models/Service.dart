import 'dart:developer';

class Service {
  int id;
  String name;
  String description;
  double total_cost;
  double price;

  Service({this.id, this.name, this.description, this.total_cost, this.price});
}
